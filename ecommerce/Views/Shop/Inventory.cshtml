@model List<ecommerce.Models.Product>

@foreach (var item in Model)
{
<div>
    <h2>@item.Name</h2>
    <img src="@item.Image" />
    <p>$@item.Price</p>
    <p><a asp-controller="Shop" asp-action="Detail" asp-route-id="@item.ID">View Detail</a></p>
</div>
}